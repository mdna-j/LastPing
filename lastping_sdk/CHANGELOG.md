# SDK Changelog

## 0.1.1
- Python: add client context manager and run-with-heartbeat helper.
- Java: add timestamp helpers, user-agent, error-capture helpers, and tests.
- Java: add Gradle + Maven build files and GitHub Packages publish support.

## 0.1.0
- Initial Python SDK (heartbeat + webhook helpers).
- Initial Java client (basic heartbeat/event helpers).
