(function(){
  // Minimal Chart.js stub for offline/air-gapped deployments.
  // This provides a no-op `Chart` constructor so the UI does not break.
  if(typeof window === 'undefined') return;
  window.Chart = function(ctx, cfg){
    this.ctx = ctx; this.cfg = cfg;
    this.destroy = function(){};
  };
})();
